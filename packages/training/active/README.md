# Active checkpoint

No checkpoint has been promoted. The experimental candidate is tracked separately under `../candidates/`, selected by `../candidate.json`.

Only `pnpm model:promote` may write an active release. Independent gold evidence, browser parity, engineering budgets and clean reproduction must pass first. A functioning experimental build is not evidence that these release requirements have been met.
